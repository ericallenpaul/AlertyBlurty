ALERTYBLURTY_IMAGE=ericallenpaul/alertyblurty:latest
# Host port for the web UI and API. Change this before `docker compose up -d` if the port is already in use.
# The container always listens on 8080; this controls the host-side published port.
ALERTYBLURTY_PORT=18080
ALERTYBLURTY_CONFIG_PATH=/app/data/appsettings.Local.json

POSTGRES_DB=alertyblurty
POSTGRES_USER=alerty_app
POSTGRES_PASSWORD=change-this-postgres-password

# Optional env-var startup override. Leave blank to use the first-run wizard.
CONNECTION_STRING=
JWT_SECRET=
JWT_ISSUER=AlertyBlurty
JWT_AUDIENCE=AlertyBlurty
JWT_EXPIRY_HOURS=24
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
